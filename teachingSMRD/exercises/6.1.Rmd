---
output: html_document
---

__For the $LOGLOGIS(\mu, \sigma)$ distribution with cdf__
$$
 F(t)=\Phi_{LOGIS} \left [ \frac{\log(t)-\mu}{\sigma} \right ], \quad t > 0; \quad -\infty< \mu <\infty, \sigma > 0,  	  
$$ 	  

a. __Find the probability scales that will linearize all of the cdfs in the logistic family.__

`r teachingSMRD:::solution()$begin`
`r teachingSMRD:::solution()$end`

a. __Use the scales to generate a proper labeled graph, and display the $LOGLOGIS(1, 1)$ and the $LOGLOGIS(1, 2)$ cdfs.__

`r teachingSMRD:::solution()$begin`
`r teachingSMRD:::solution()$end`

a. __What quantile of this distribution corresponds to the scale parameter $\exp(\mu)$?__

`r teachingSMRD:::solution()$begin`
`r teachingSMRD:::solution()$end`
