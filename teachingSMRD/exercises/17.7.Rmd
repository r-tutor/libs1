---
output: html_document
---

__Use the results in Table 17.2 for Example 17.9 to do the following:__

a. __Compute an $90\%$ normal-approximation confidence interval for $\beta_{1}^{[\sigma]}$.__

`r teachingSMRD:::solution()$begin`
`r teachingSMRD:::solution()$end`

a. __Explain how this confidence interval can be used to judge whether $\sigma$ depends on the level of pseudo stress or not.__

`r teachingSMRD:::solution()$begin`
`r teachingSMRD:::solution()$end`

a. __Do a likelihood ratio test to determine whether or not there exists strong evidence that $\sigma$ depends on the level of pseudo stress.__

`r teachingSMRD:::solution()$begin`
`r teachingSMRD:::solution()$end`

a. __Explain the steps you would follow to compute a likelihood-ratio-based confidence interval for $\beta_{1}^{[\sigma]}$.__

`r teachingSMRD:::solution()$begin`
`r teachingSMRD:::solution()$end`
