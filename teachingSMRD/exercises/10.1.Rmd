---
output: html_document
---

__Use the input information in Example 10.1 to compute the planning values $\mu^{\Box}$ and $\sigma^{\Box}$ for both the lognormal and the Weibull distributions. Plot these cdfs on appropriate probability paper.  Also, compute the standardized censoring times $\zeta_{c}$.__

`r teachingSMRD:::solution()$begin`
`r teachingSMRD:::solution()$end`
